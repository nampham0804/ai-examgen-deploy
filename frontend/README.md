# Frontend

This folder is reserved for the user-facing web application.

Recommended starting options:

- React or Next.js for a production-style UI.
- Streamlit for a quick internal demo.

Keep frontend dependencies and build scripts inside this folder so deployment can
be handled separately from the FastAPI backend.

